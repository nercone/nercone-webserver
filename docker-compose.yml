services:
  website:
    container_name: website
    build: .
    network_mode: host
    restart: unless-stopped
    volumes:
      - "./.git:/srv/website/.git:ro"
      - "./databases:/srv/website/databases"
      - "./logs:/srv/website/logs:ro"
      - "./public:/srv/website/public:ro"
    environment:
      - DATABASE_URL=postgresql://website:website@postgres:5432/website
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:17-alpine
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: website
      POSTGRES_PASSWORD: website
      POSTGRES_DB: website
    volumes:
      - ./databases/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U website -d website"]
      interval: 10s
      timeout: 5s
      retries: 5
