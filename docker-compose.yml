services:
  website:
    container_name: website
    build: .
    restart: unless-stopped
    volumes:
      - "./.git:/srv/website/.git:ro"
      - "./databases:/srv/website/databases"
      - "./logs:/srv/website/logs"
      - "./public:/srv/website/public"
    ports:
      - "8080:8080"
